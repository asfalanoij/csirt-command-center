version: '3.8'

services:
  csirt-dashboard:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    restart: unless-stopped
    networks:
      - csirt-network

networks:
  csirt-network:
    driver: bridge